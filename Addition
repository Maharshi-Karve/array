#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int sum = 0;
    int i;

    for(i = 0; i <= 5; i++) {   // Error: should be i < 5
        sum += arr[i];
    }

    printf("Sum of array elements is: %d\n", sum)
    return 0;
}
